query GetUserParties($userId: Long!) {
    getAllParties(userId: $userId) {
        id
        name
        description
        locationName
        owner {
            name
        }
        type
    }
}
