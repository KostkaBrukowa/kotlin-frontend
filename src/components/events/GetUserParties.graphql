query GetUserParties($userId: String!) {
  getAllParties(userId: $userId) {
    id
    name
    description
    locationName
    type

    owner {
      name
    }

    partyParticipants {
      id
      name
    }
  }
}
