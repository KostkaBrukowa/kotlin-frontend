query GetUserParties($userId: String!) {
  getAllParties(userId: $userId) {
    id
    name
    description
    locationName
    owner {
      name
    }
    type
    partyParticipants {
      id
      name
    }
  }
}
