query GetUserExpenses($userId: String!) {
    getExpensesForUser(userId: $userId) {
        id
        amount
        description
        name
        expenseStatus
    }

    getClientsPayments(userId: $userId) {
        id
        amount
        status
        paymentExpense {
            id
            description
            name
            expenseStatus
        }
    }
}
